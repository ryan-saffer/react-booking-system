---
import { Image } from "astro:assets";

type Props = {
  color: string;
  image: ImageMetadata;
  imageAlt: string;
  title: string;
  path: string;
};

const { color, image, imageAlt, path, title } = Astro.props;
---

<style define:vars={{ color: color }}>
  .package:hover {
    border: 1px solid var(--color);
  }
</style>

<div
  class="package flex max-w-96 cursor-pointer flex-col items-center rounded-2xl border border-transparent py-0 hover:border-solid"
>
  <a href={path}>
    <Image src={image} alt={imageAlt} class="w-full min-w-52 object-contain" />
  </a>
  <a
    href={path}
    class="my-4 text-center font-lilita text-2xl sm:text-3xl"
    style={{ color }}
  >
    {title}
  </a>
  <a
    href={path}
    class="pb-1 font-gotham text-sm font-semibold uppercase sm:text-base"
    style={{ color }}
  >
    Learn more
  </a>
</div>
