---
import FlipCreation from "../flip-creation.astro";

import { sbCreationsSparklingLipBalmPng as creation } from "@/data/storyblok-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<!--Exact same as sparkling lip balm, just different text -->
<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Fairy Lip Balm"]}
  color="purple"
/>
