---
import FlipCreation from "../flip-creation.astro";

import { sbCreationsUnicornBathCrumblePng as creation } from "@/data/storyblok-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Unicorn Fizz Crumble"]}
  color="blue"
/>
