---
import FlipCreation from "../flip-creation.astro";

import { sbCreationsTsFacePaintPng as creation } from "@/data/storyblok-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["'Lover' Glitter Face Paint"]}
  color="blue"
/>
