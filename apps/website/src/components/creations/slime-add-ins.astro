---
import FlipCreation from "../flip-creation.astro";

import { sbCreationsSlimeAddInsPng as creation } from "@/data/storyblok-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Choose your add ins"]}
  color="white"
/>
