---
import FlipCreation from "../flip-creation.astro";

import { sbCreationsFairyBathBombsPng as creation } from "@/data/storyblok-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Fairy Bath Bombs"]}
  color="purple"
/>
