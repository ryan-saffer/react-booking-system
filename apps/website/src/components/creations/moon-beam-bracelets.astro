---
import FlipCreation from "../flip-creation.astro";

import { sbCreationsMoonBeamBraceletsPng as creation } from "@/data/storyblok-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Moon Beam Bracelets"]}
  color="pink"
/>
