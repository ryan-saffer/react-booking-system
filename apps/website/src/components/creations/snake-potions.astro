---
import FlipCreation from "../flip-creation.astro";

import { sbCreationsSnakePotionsPng as creation } from "@/data/storyblok-images";

type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Slithering Snake Potions"]}
  color="green"
/>
