---
import FlipCreation from "../flip-creation.astro";

import { sbCreationsHeroPowerChargersPng as creation } from "@/data/storyblok-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Hero Power Chargers"]}
  color="blue"
/>
