---
import FlipCreation from "../flip-creation.astro";

import { sbCreationsGlowCrownsPng as creation } from "@/data/storyblok-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Glow Crowns"]}
  color="purple"
/>
