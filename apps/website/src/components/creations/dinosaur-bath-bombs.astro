---
import FlipCreation from "../flip-creation.astro";

import { sbCreationsDinosaurBathBombsPng as creation } from "@/data/storyblok-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Dinosaur Bath Bombs"]}
  color="green"
/>
