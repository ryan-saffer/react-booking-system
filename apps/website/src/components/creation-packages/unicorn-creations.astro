---
import CreationsSummary from "@/components/creations-summary.astro";
import Creations from "@/components/party-package-creations.astro";
import FairyWands from "@/components/creations/fairy-wands.astro";
import UnicornLipBalm from "@/components/creations/unicorn-lip-balm.astro";
import UnicornBathCrumble from "@/components/creations/unicorn-bath-crumble.astro";
import UnicornSoap from "@/components/creations/unicorn-soap.astro";
import UnicornBathBombs from "@/components/creations/unicorn-bath-bombs.astro";
import UnicornCloudSlime from "@/components/creations/unicorn-cloud-slime.astro";
import GlowCrowns from "@/components/creations/glow-crowns.astro";

type Props = {
  variant?: "summary" | "party";
  image?: ImageMetadata;
  imageAlt?: string;
};

const {
  variant = "summary",
  image,
  imageAlt = "Unicorn Party Package",
} = Astro.props;

const creations = [
  FairyWands,
  UnicornLipBalm,
  UnicornBathCrumble,
  UnicornSoap,
  UnicornBathBombs,
  UnicornCloudSlime,
  GlowCrowns,
];
---

{
  variant === "party" ? (
    <Creations
      partyPackage="Unicorn"
      image={image!}
      imageAlt={imageAlt}
      useDefaultSlot
    >
      {creations.map((Creation) => (
        <div>
          <Creation flippable={false} />
        </div>
      ))}
    </Creations>
  ) : (
    <CreationsSummary title="Unicorn Creations" color="#F24DA2" useDefaultSlot>
      {creations.map((Creation) => (
        <Creation flippable={false} />
      ))}
    </CreationsSummary>
  )
}
