---
import CreationsSummary from "@/components/creations-summary.astro";
import Creations from "@/components/party-package-creations.astro";
import type { ImageSource } from "@/types/images";
import FairyWands from "@/components/creations/fairy-wands.astro";
import PixieGlitter from "@/components/creations/pixie-glitter.astro";
import FairyBracelets from "@/components/creations/fairy-bracelets.astro";
import FairyLipBalm from "@/components/creations/fairy-lip-balm.astro";
import FairySlime from "@/components/creations/fairy-slime.astro";
import UnicornSoap from "@/components/creations/unicorn-soap.astro";
import FairyBathBombs from "@/components/creations/fairy-bath-bombs.astro";
import MarshmallowSlime from "@/components/creations/marshmallow-slime.astro";
import FairyHairShimmer from "@/components/creations/fairy-hair-shimmer.astro";
import SparkleCrowns from "@/components/creations/sparkle-crowns.astro";

type Props = {
  variant?: "summary" | "party";
  image?: ImageSource;
  imageAlt?: string;
};

const {
  variant = "summary",
  image,
  imageAlt = "Fairy Party Package",
} = Astro.props;

const creations = [
  FairyWands,
  PixieGlitter,
  FairyBracelets,
  FairyLipBalm,
  FairySlime,
  UnicornSoap,
  FairyBathBombs,
  MarshmallowSlime,
  FairyHairShimmer,
  SparkleCrowns,
];
---

{
  variant === "party" ? (
    <Creations
      partyPackage="Fairy"
      image={image!}
      imageAlt={imageAlt}
      useDefaultSlot
    >
      {creations.map((Creation) => (
        <div>
          <Creation flippable={false} />
        </div>
      ))}
    </Creations>
  ) : (
    <CreationsSummary title="Fairy Creations" color="#4ED85F" useDefaultSlot>
      {creations.map((Creation) => (
        <Creation flippable={false} />
      ))}
    </CreationsSummary>
  )
}
