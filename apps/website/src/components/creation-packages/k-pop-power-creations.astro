---
import CreationsSummary from "@/components/creations-summary.astro";
import Creations from "@/components/party-package-creations.astro";
import StarhexWands from "@/components/creations/starhex-wands.astro";
import SquishyPockets from "@/components/creations/squishy-pockets.astro";
import HendrixGlitterShine from "@/components/creations/hendrix-glitter-shine.astro";
import GoldenSlime from "@/components/creations/golden-slime.astro";
import MoonBeamBracelets from "@/components/creations/moon-beam-bracelets.astro";
import HeroPowerChargers from "@/components/creations/hero-power-chargers.astro";
import CharmKeyrings from "@/components/creations/charm-keyrings.astro";
import GlowCrowns from "@/components/creations/glow-crowns.astro";

type Props = {
  variant?: "summary" | "party";
  image?: ImageMetadata;
  imageAlt?: string;
};

const {
  variant = "summary",
  image,
  imageAlt = "K-Pop Power Party Package",
} = Astro.props;

const creations = [
  StarhexWands,
  SquishyPockets,
  HendrixGlitterShine,
  GoldenSlime,
  HeroPowerChargers,
  MoonBeamBracelets,
  CharmKeyrings,
  GlowCrowns,
];
---

{
  variant === "party" ? (
    <Creations
      partyPackage="K-Pop Power"
      image={image!}
      imageAlt={imageAlt}
      useDefaultSlot
    >
      {creations.map((Creation) => (
        <div>
          <Creation flippable={false} />
        </div>
      ))}
    </Creations>
  ) : (
    <CreationsSummary
      title="Kpop Power Creations"
      color="#F24DA2"
      useDefaultSlot
    >
      {creations.map((Creation) => (
        <Creation flippable={false} />
      ))}
    </CreationsSummary>
  )
}
