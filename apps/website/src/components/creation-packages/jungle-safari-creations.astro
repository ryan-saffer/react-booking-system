---
import CreationsSummary from "@/components/creations-summary.astro";
import Creations from "@/components/party-package-creations.astro";
import type { ImageSource } from "@/types/images";
import MonsterSlimePurple from "@/components/creations/monster-slime-purple.astro";
import BugsInBathBombs from "@/components/creations/bugs-in-bath-bombs.astro";
import MonsterSlimeGreen from "@/components/creations/monster-slime-green.astro";
import BubblingVolcanoes from "@/components/creations/bubbling-volcanoes.astro";
import MonsterBrains from "@/components/creations/monster-brains.astro";
import FirePotions from "@/components/creations/fire-potions.astro";
import SnakePotions from "@/components/creations/snake-potions.astro";
import DinosaurBathBombs from "@/components/creations/dinosaur-bath-bombs.astro";

type Props = {
  variant?: "summary" | "party";
  image?: ImageSource;
  imageAlt?: string;
};

const {
  variant = "summary",
  image,
  imageAlt = "Jungle Safari Party Package",
} = Astro.props;

const creations = [
  MonsterSlimePurple,
  MonsterBrains,
  BugsInBathBombs,
  MonsterSlimeGreen,
  DinosaurBathBombs,
  BubblingVolcanoes,
  FirePotions,
  SnakePotions,
];
---

{
  variant === "party" ? (
    <Creations
      partyPackage="Jungle Safari"
      image={image!}
      imageAlt={imageAlt}
      useDefaultSlot
    >
      {creations.map((Creation) => (
        <div>
          <Creation flippable={false} />
        </div>
      ))}
    </Creations>
  ) : (
    <CreationsSummary
      title="Jungle Safari Creations"
      color="#4ED85F"
      useDefaultSlot
    >
      {creations.map((Creation) => (
        <Creation flippable={false} />
      ))}
    </CreationsSummary>
  )
}
